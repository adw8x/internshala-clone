{
  "name": "internshala-clone",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix internarea run dev",
    "build": "npm --prefix internarea run build",
    "start": "npm --prefix internarea run start",
    "lint": "npm --prefix internarea run lint"
  },
  "dependencies": {},
  "devDependencies": {}
}